extends layout

block content
  //- Feature Films Section
  section.jumbotron.text-center
    .container
      h1.jumbotron-heading-text= pageHeader.heading
      br
      p.lead.text-white= pageHeader.tagline
      p.lead.text-white= pageHeader.statement
      br
      br
      .container
        .row.justify-content-center.bg-black
          each movie in movies
            .col-md-8.col-lg-8
              br
              .card.mw-100.mh-75.bg-dark
                br
                h3.card-title-text.text-warning.text-center= movie.title
                img.img-responsive.mx-auto.d-block(style="width:40%" src= movie.poster)
                br
                h4.card-title-text.text-warning.text-center Film ##{movie.id}
                a.btn-link(href="/movies")
                  button.btn.btn-success(type="button")= movie.details
                  br
                br 